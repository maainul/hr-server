export const generateSalaryCtrl = async (req, res) => {
    try {

        // add month as a query params for generate salary
        // retrieve employeeId and gross = grossSalary
        // add  employee = empid from  EmployeeSalaryModel and grossSalary = grossSalary

        // go to deduction model and sum all deduction and put in the totalDeductions

        // neSalary = grossSalary + totalDeductions + salaryIncrementMaterial 

        // generate salary

    } catch (error) {

    }
}


export const payrollListCtrl = async (req, res) => {
    try {

        // after generate salary show data from table.

    } catch (error) {

    }
}